// Deprecated: the app is now modular under `renderer/`.
// Keep a small stub to avoid accidental global bootstrapping.
console.warn('renderer.js is deprecated — use renderer/main.js and renderer/app.js');
